<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu Page</title>
</head>
<body>
    <h1>Menu Page</h1>
    <a href="/searchMovie/<%= user %>/<%= pwd %>">Search for movies</a><br>
    <a href="/createMovie/<%= user %>/<%= pwd %>">Create new movie</a><br>
    <% if(pwd!= undefined){ %>
    <% if(user == "carmel" &&  pwd == "123") { %>
    
        <!-- this link is only for admins to see -->
        <a href="/userManagement/<%= user %>/<%= pwd %>">Edit Users</a>    
    
    <%}}%>
    

</body>
</html>